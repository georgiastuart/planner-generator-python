<div class="grid grid-cols-3 gap-8">
<div class="flex flex-col col-span-2">
<div class="weekly-goals-header"><a href="#{{ first.strftime('%Y-W%W') }}">{{ first.strftime('%Y W%-W') }}: {{ first.strftime('%-d %b').lower() }} - {{ last.strftime('%-d %b').lower() }} work goals</a></div>
{% for i in range(25) %}
<div class="lines h-5mm"></div>
{% endfor %}
</div>
<div class="flex flex-col">
<div class="reflection-header w-full"><div class="text-end w-full">reflections</div></div>
{% for i in range(25) %}
<div class="lines h-5mm"></div>
{% endfor %}
</div>
<div class="col-span-2 h-5mm"></div>
</div>
