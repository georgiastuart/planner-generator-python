<div class="flex flex-col">
<div class="annual-goals-header"><div class="mx-auto"><a href="#{{ year }}">{{ year }} goals</a></div></div>
<div class="grid grid-cols-2 gap-6">
{% for area in areas_of_focus %}
<div class="flex flex-col">
<div class="area-header"><i class="{{ area[1] }} pr-2"></i>{{ area[0].lower() }}</div>
{% for i in range(10) %}
<div class="border-b border-slate-800 h-5mm"></div>
{% endfor %}
</div>
{% endfor %}
</div>
</div>
