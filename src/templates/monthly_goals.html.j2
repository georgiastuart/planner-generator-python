<div class="flex flex-col">
<div class="annual-goals-header"><div class="mx-auto"><a href="#{{ first.strftime('%Y-%m') }}">{{ first.strftime('%B %Y').lower() }} goals</a></div></div>
<div class="grid grid-cols-2 gap-6">
{% for area in areas_of_focus %}
<div class="flex flex-col">
<div class="area-header flex items-center"><div class="align-middle"><i class="{{ area[1] }} pr-2 text-2xl"></div></i><div>{{ area[0].lower() }}</div></div>
{% for i in range(10) %}
<div class="lines h-5mm"></div>
{% endfor %}
</div>
{% endfor %}
</div>
</div>
